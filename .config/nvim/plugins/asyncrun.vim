UsePlugin 'asyncrun.vim'

" This plugin takes the advantage of new APIs in Vim 8 (and NeoVim) 
" to enable you to run shell commands in the background and read the output in the quickfix window in realtime:

let g:asyncrun_open=1
